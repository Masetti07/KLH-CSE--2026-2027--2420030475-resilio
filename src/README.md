# Application Source

- `backend/` contains the Day 1 FastAPI image-processing service and tests.
- `frontend/` contains the Day 1 React/Vite interface.

The renderer-independent structural model returned by the backend is the source of truth. The current interface displays its detected candidates but does not edit them.
